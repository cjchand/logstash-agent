# Grok pattern match for httpd Logs
filter {
  if [type] == "httpd" {
    grok {
      match => { "message" => '%{COMMONAPACHELOG}' }
      match => { "message" => '%{COMBINEDAPACHELOG}' }
      break_on_match => false
      tag_on_failure => []
    }
  }
}
